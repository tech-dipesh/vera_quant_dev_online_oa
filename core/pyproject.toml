[project]
name = "quant-system-core"
version = "0.1.0"
requires-python = ">=3.11,<3.12"
dependencies = [
    "pandas>=2.2",
    "pandas-ta>=0.4.71b0",
    "numpy>=1.26",
    "fastapi>=0.115",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
